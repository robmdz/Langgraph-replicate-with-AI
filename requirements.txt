# Requirements for File Agent
# For Poetry users, use: poetry install
# For pip users, use: pip install -r requirements.txt

langchain>=0.1.0
langgraph>=0.0.20
langchain-openai>=0.0.5
typer[all]>=0.9.0
rich>=13.7.0
python-dotenv>=1.0.0
